describe('InventoryDb', () => {
  it('should work', () => {
    // new PrismaClient().category.create({
    //   data: {
    //     name: '',
    //     parent: { connect: { id: 1 }, create: { name: '' } },
    //     children: {
    //       connect: [{ id: 1 }, { id: 2 }],
    //       createMany: { data: [{ name: '' }, { name: '' }] },
    //     },

    //   },
    // });
    // new PrismaClient().category.findMany({

    //   select:{
    //     parent:{},
    //     children:{distinct,select,where, take , skip, orderBy,}}

    // });
    expect(1).toEqual(1);
  });
});
